<%- include('partials/header') %>

<section class="container-full">
  <div class="form-container-modern">
    <div class="form-header">
      <span class="form-icon">✏️</span>
      <h2>Editar Película</h2>
      <p class="form-subtitle">Modifica la información de la película</p>
    </div>

    <% if (msg) { %>
      <div class="alert <%= color === 'green' ? 'alert-success' : 'alert-error' %>">
        <span class="alert-icon"><%= color === 'green' ? '✓' : '✕' %></span>
        <%= msg %>
      </div>
    <% } %>

    <form action="/movies/edit/<%= movie._id.toString() %>" method="POST" class="modern-form">
      <div class="form-row">
        <div class="form-group">
          <label for="title">📽️ Título*</label>
          <input type="text" id="title" name="title" required maxlength="60" value="<%= movie.title || '' %>" placeholder="Ingresa el título de la película">
        </div>
      </div>

      <div class="form-row form-row-2">
        <div class="form-group">
          <label for="year">📅 Año</label>
          <input type="number" id="year" name="year" min="1800" max="2100" value="<%= movie.year || (movie.released ? new Date(movie.released).getFullYear() : '') %>" placeholder="2024">
        </div>

        <div class="form-group">
          <label for="type">🎭 Tipo*</label>
          <input type="text" id="type" name="type" placeholder="movie, series, etc." required value="<%= movie.type || '' %>">
        </div>
      </div>

      <div class="form-group">
        <label for="director">🎥 Director*</label>
        <input type="text" id="director" name="director" placeholder="Christopher Nolan" required value="<%= movie.directors ? movie.directors.join(', ') : '' %>">
        <small class="form-hint">Separa múltiples directores con comas</small>
      </div>

      <div class="form-group">
        <label for="genres">🏷️ Géneros</label>
        <input type="text" id="genres" name="genres" placeholder="Drama, Action, Thriller" value="<%= movie.genres ? movie.genres.join(', ') : '' %>">
        <small class="form-hint">Separa múltiples géneros con comas</small>
      </div>

      <div class="form-group">
        <label for="urlImage">🖼️ URL del Poster*</label>
        <input type="url" id="urlImage" name="urlImage" required placeholder="https://example.com/poster.jpg" value="<%= movie.poster || '' %>">
      </div>

      <div class="form-group">
        <label for="synopsis">📝 Sinopsis</label>
        <textarea id="synopsis" name="synopsis" rows="5" placeholder="Describe la trama de la película..."><%= movie.plot || movie.fullplot || '' %></textarea>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn-submit-modern">💾 Guardar Cambios</button>
        <a href="/" class="btn-cancel-modern">Cancelar</a>
      </div>
      
      <p class="form-note">* Campos obligatorios</p>
    </form>
  </div>
</section>

<%- include('partials/footer') %>
